import React from 'react';

const filterReducer = ({ state, action }) => {
    switch (action.type) {
        
    }
    return (
        <div>
            
        </div>
    );
};

export default filterReducer;